'use client';

import { Box, Typography, Stack } from '@mui/material';

export default function Selector() {
  return (
    <Stack direction="column">
      <Typography variant="h2">Select a story</Typography>
    </Stack>
  );
}